# LangChain 源码翻译工作流

> 本文档定义了 LangChain 源码注释翻译的标准流程，用于后续批量翻译工作。

## 翻译原则

### 1. 术语一致性
- 所有术语必须使用 `关键词翻译对照表.md` 中的对应中文翻译
- 类名和接口名保留英文原名，用中文注释解释含义
- 首次出现的术语附英文原词，如：`可运行单元（Runnable）`

### 2. 注释翻译规则
- **Docstring**: 翻译为中文，放在英文原文之后，或替换英文
- **行内注释**: 翻译为中文
- **模块顶部注释**: 翻译为中文
- **类型注解中的注释**: 翻译为中文

### 3. 提示词翻译规则
- 如果代码中包含英文提示词（prompt），在附近添加中文翻译注释
- 格式：`# 中文翻译: xxx`
- 保留英文原文以便调试

### 4. 保留内容（不翻译）
- 代码标识符（类名、函数名、变量名）
- 类型注解
- 导入语句
- 配置文件内容
- 调试用的打印语句

## 翻译流程

### 步骤 1: 准备工作
```bash
# 1. 确保在项目根目录
cd D:\codes\langchain-zh

# 2. 读取关键词翻译对照表
cat 关键词翻译对照表.md

# 3. 确定要翻译的目录
# 例如: libs/core/langchain_core/runnables
```

### 步骤 2: 读取源文件
```bash
# 读取目标文件，了解其结构
cat libs/core/langchain_core/runnables/base.py
```

### 步骤 3: 翻译注释
按照以下顺序翻译：
1. 模块顶部 docstring
2. 类和函数的 docstring
3. 行内注释
4. 类型注解中的字符串

### 步骤 4: 检查提示词
搜索以下模式：
- `"""` 块中的英文文本
- `prompt` 相关的字符串
- `system` 消息内容
- 错误消息

### 步骤 5: 验证
- 运行 `make lint` 确保代码风格正确
- 运行 `uv run --group lint mypy .` 确保类型正确

## 快速开始（后续翻译）

后续翻译只需执行：

```bash
# 告诉 Sisyphus 要翻译的目录，例如：
# "请翻译 libs/core/langchain_core/messages 目录的注释"

# Sisyphus 会自动：
# 1. 读取关键词翻译对照表
# 2. 读取源文件
# 3. 翻译所有注释
# 4. 添加提示词的中文翻译
# 5. 验证代码正确性
```

## 文件处理清单

对于每个 `.py` 文件，执行以下检查：

- [ ] 模块 docstring
- [ ] `__all__` 导出列表（添加中文注释）
- [ ] 每个类的 docstring
- [ ] 每个方法的 docstring
- [ ] 行内注释
- [ ] 类型注解中的字符串
- [ ] 函数/方法参数说明（Args）
- [ ] 返回值说明（Returns）
- [ ] 异常说明（Raises）
- [ ] 示例代码（翻译注释，不翻译代码）

## 示例

### 翻译前
```python
class AgentAction(Serializable):
    """Represents a request to execute an action by an agent.

    The action consists of the name of the tool to execute and the input to pass
    to the tool. The log is used to pass along extra information about the action.
    """
```

### 翻译后
```python
class AgentAction(Serializable):
    """表示 Agent 执行动作的请求。

    该动作包含要执行的工具名称和传递给工具的输入。
    日志（log）用于传递关于该动作的额外信息。

    Args:
        tool: 要执行的工具名称。
        tool_input: 传递给工具的输入。
        log: 关于该动作的额外信息。
    """
```

### 提示词翻译示例

### 翻译前
```python
system_message = "You are a helpful assistant."
```

### 翻译后
```python
# 中文翻译: 你是一个有帮助的助手。
system_message = "You are a helpful assistant."
```

## 常见问题

### Q: 如何处理多行 docstring？
A: 将整个 docstring 翻译为中文，保持格式。

### Q: 示例代码需要翻译吗？
A: 示例代码中的注释翻译，但代码本身保持不变。

### Q: 类型注解中的字符串如何翻译？
A: 如果是用户可见的消息（如错误提示），翻译；如果是内部标识符，不翻译。

### Q: `Runnable` 这类核心术语如何处理？
A: 首次出现时附上中文：`可运行单元（Runnable）`，后续可直接使用中文。

## 相关文件

- `关键词翻译对照表.md` - 术语翻译参考
- `源码阅读指南.md` - 代码结构参考
