# LangChain 源码阅读指南

## 项目概述

LangChain 是一个用于构建 LLM 应用和 Agent 的框架。本项目采用 monorepo 结构，主要包位于 `libs/` 目录下。

## 目录结构

```
langchain-zh/
├── libs/
│   ├── core/              # langchain-core - 核心抽象和基础接口
│   ├── langchain/         # langchain-classic (遗留包，不新增功能)
│   ├── langchain_v1/      # langchain - 当前活跃维护的主包
│   ├── partners/          # 第三方集成 (OpenAI, Anthropic, Ollama 等)
│   ├── text-splitters/    # 文档分块工具
│   └── standard-tests/    # 集成测试标准套件
```

## 推荐阅读顺序

### 第一阶段：核心抽象层 (`libs/core/langchain_core/`)

从底层开始，理解 LangChain 的核心抽象。这是所有上层功能的基础。

| 模块 | 文件 | 说明 |
|------|------|------|
| **Runnable 协议** | `runnables/base.py` | `Runnable` - 整个框架的核心接口 |
| **消息系统** | `messages/` | 各种消息类型和内容块 |
| **工具系统** | `tools/base.py` | `BaseTool` - 工具的定义和规范 |
| **检索器** | `retrievers.py` | `BaseRetriever` - 文档检索的抽象接口 |
| **输出解析** | `output_parsers/` | 将 LLM 输出解析为结构化格式 |
| **Prompt 模板** | `prompts/` | Prompt 模板的各种实现 |
| **向量存储** | `vectorstores/` | 向量数据库抽象接口 |
| **嵌入模型** | `embeddings/` | 嵌入模型抽象接口 |
| **存储** | `stores.py` | `BaseStore` - 键值存储抽象 |
| **文档** | `documents/` | `Document`、`Blob` 等 |

### 第二阶段：主包实现层 (`libs/langchain_v1/langchain/`)

基于 core 抽象的具体实现。

| 模块 | 文件 | 说明 |
|------|------|------|
| **Agent 工厂** | `agents/factory.py` | `create_agent` 函数 - 创建 Agent 的主要入口 |
| **Agent 状态** | `agents/middleware/types.py` | `AgentState` - Agent 的中间件架构 |
| **中间件** | `agents/middleware/` | Agent 行为中间件（工具选择、重试等） |
| **结构化输出** | `agents/structured_output.py` | Agent 输出结构化结果的支持 |
| **聊天模型** | `chat_models/` | 聊天模型集成实现 |

**重要**：LangChain Agent 使用 `create_agent` 工厂函数创建，而不是类名为 `AgentExecutor` 的类。

### 第三阶段：Partner 集成 (`libs/partners/`)

第三方服务集成，学习如何对接不同的 LLM Provider。

| 目录 | 说明 |
|------|------|
| `openai/` | OpenAI GPT 系列模型集成 |
| `anthropic/` | Anthropic Claude 模型集成 |
| `ollama/` | 本地模型支持 |

### 第四阶段：辅助模块

| 模块 | 位置 | 说明 |
|------|------|------|
| **文本分块** | `libs/text-splitters/` | 文档处理和分块策略 |
| **回调系统** | `langchain_core/callbacks/` | 事件回调和监控 |
| **文档加载器** | `langchain_core/document_loaders/` | 各种文档格式加载器 |

## 核心概念阅读顺序

### 1. Runnable 协议 (必须首先理解)

```
libs/core/langchain_core/runnables/base.py
```

这是 LangChain 最核心的概念，所有组件都实现 `Runnable` 接口：

| 方法 | 说明 |
|------|------|
| `invoke()` / `ainvoke()` | 单次调用 |
| `batch()` / `abatch()` | 批量调用 |
| `stream()` / `astream()` | 流式调用 |
| `pipe()` / `| ` | 管道操作（LCEL） |

**关键实现类：**
- `RunnableSequence` - 顺序执行
- `RunnableParallel` - 并行执行
- `RunnablePassthrough` - 直通传递
- `RunnableWithFallbacks` - 降级处理
- `RunnableBranch` - 条件分支
- `RunnableBinding` - 绑定配置

### 2. 消息系统

```
libs/core/langchain_core/messages/
```

**消息类型：**
- `HumanMessage` - 用户消息
- `AIMessage` - AI 响应
- `SystemMessage` - 系统提示
- `ToolMessage` - 工具执行结果
- `FunctionMessage` - 函数调用消息
- `ChatMessage` - 通用聊天消息

**内容块（ContentBlock）：**
- `TextContentBlock` - 文本内容
- `ImageContentBlock` - 图片内容
- `AudioContentBlock` - 音频内容
- `VideoContentBlock` - 视频内容
- `ReasoningContentBlock` - 推理内容（思考过程）

### 3. Agent 架构

```
libs/langchain_v1/langchain/agents/
```

**重要提示**：LangChain Agent 不是通过类直接实例化，而是使用 `create_agent` 工厂函数：

```python
from langchain.agents import create_agent

agent = create_agent(chat_model, tools, prompt)
```

**Agent 状态 (`AgentState`)：**
- 使用 TypedDict 定义状态结构
- 通过中间件（Middleware）扩展行为

**中间件类型：**
- `tool_selection.py` - 工具选择
- `tool_retry.py` - 工具调用重试
- `human_in_the_loop.py` - 人工介入
- `model_retry.py` - 模型调用重试

**注意**：Agent 底层基于 **LangGraph** 构建，提供持久化和状态管理。

### 4. 工具系统

```
libs/core/langchain_core/tools/base.py
```

**工具相关类：**
- `BaseTool` - 工具基类
- `BaseToolkit` - 工具包基类
- `Tool` - 简单工具实现
- `StructuredTool` - 结构化工具
- `@tool` 装饰器 - 函数转工具

**相关函数：**
- `create_schema_from_function` - 从函数生成 schema
- `convert_runnable_to_tool` - 将 Runnable 转为工具
- `create_retriever_tool` - 从检索器创建工具

### 5. 输出解析

```
libs/core/langchain_core/output_parsers/
```

**解析器类：**
- `BaseOutputParser` - 基类
- `StrOutputParser` - 字符串输出
- `JsonOutputParser` / `SimpleJsonOutputParser` - JSON 输出
- `PydanticOutputParser` - Pydantic 模型输出
- `PydanticToolsParser` - Pydantic 工具参数解析
- `XMLOutputParser` - XML 输出

### 6. 存储系统

```
libs/core/langchain_core/stores.py
```

- `BaseStore` - 键值存储抽象接口

### 7. 回调系统

```
libs/core/langchain_core/callbacks/base.py
```

- `BaseCallbackHandler` - 回调处理器基类
- `CallbackManager` - 回调管理器
- 各种具体 Handler：`StdOutCallbackHandler`、`FileCallbackHandler` 等

## 关键文件清单

### 核心入口文件

| 文件 | 用途 |
|------|------|
| `libs/core/langchain_core/__init__.py` | Core 包导出 |
| `libs/langchain_v1/langchain/__init__.py` | 主包导出 |
| `libs/core/langchain_core/_import_utils.py` | 兼容性导入工具 |

### 重要抽象类

| 文件 | 类 | 用途 |
|------|------|------|
| `runnables/base.py` | `Runnable` | 核心协议 |
| `runnables/base.py` | `RunnableSerializable` | 可序列化运行单元 |
| `tools/base.py` | `BaseTool` | 工具基类 |
| `retrievers.py` | `BaseRetriever` | 检索器基类 |
| `messages/base.py` | `BaseMessage` | 消息基类 |
| `stores.py` | `BaseStore` | 键值存储基类 |
| `agents/factory.py` | `create_agent` | Agent 工厂函数 |

## 开发工具

```bash
# 安装依赖
uv sync --all-groups

# 运行测试
make test

# 代码检查
make lint

# 代码格式化
make format

# 类型检查
uv run --group lint mypy .
```

## 学习建议

1. **从 Runnable 开始**：这是理解整个框架的钥匙
2. **阅读 `__init__.py` 文件**：了解每个模块导出的公共 API
3. **关注 `create_agent` 工厂函数**：这是创建 Agent 的主要方式，不是类
4. **理解中间件架构**：Agent 通过中间件扩展功能
5. **使用 LSP 跳转**：理解类型继承关系
6. **运行示例代码**：理论与实践结合

## 相关资源

- [官方文档](https://docs.langchain.com/oss/python/langchain/overview)
- [API 参考](https://reference.langchain.com/python/)
- [LangChain Academy](https://academy.langchain.com/)
