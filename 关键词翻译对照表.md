# LangChain 关键词翻译对照表

> 本对照表用于确保 AI 翻译 LangChain 项目注释时的一致性。所有翻译均经过斟酌，以符合 LangChain 领域术语惯例。

## 使用说明

1. **翻译注释时**，优先使用本表中的对应中文词汇
2. **类名和接口名**建议保持英文原名，因为它们是代码标识符
3. **首次出现**的术语，在注释中可附英文原词，如：`可运行单元（Runnable）`
4. **保持一致性**：同一术语在全文中使用统一的翻译
5. **新增术语**：如果遇到表中没有的词，请根据语境判断合适的中文表达

---

## 一、核心抽象类 / Base Classes

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `Runnable` | 可运行单元 | 框架核心接口 |
| `RunnableSerializable` | 可序列化运行单元 | 支持序列化/反序列化 |
| `RunnableSequence` | 顺序运行单元 | 按顺序执行 |
| `RunnableParallel` | 并行运行单元 | 同时执行多个 |
| `RunnableBinding` | 绑定运行单元 | 绑定配置/参数 |
| `RunnableLambda` | Lambda 运行单元 | 将函数转为 Runnable |
| `RunnableGenerator` | 生成器运行单元 | 流式生成 |
| `BaseRetriever` | 检索器基类 | 文档检索抽象 |
| `BaseTool` | 工具基类 | 工具定义基类 |
| `BaseToolkit` | 工具包基类 | 工具集合基类 |
| `BaseMessage` | 消息基类 | 消息抽象基类 |
| `BaseCallbackHandler` | 回调处理器基类 | 事件回调基类 |
| `BaseChatMessageHistory` | 聊天历史基类 | 消息历史存储 |
| `BaseCache` | 缓存基类 | 缓存抽象 |
| `BaseStore` | 存储基类 | 键值存储抽象 |
| `BaseDocumentTransformer` | 文档转换器基类 | 文档处理基类 |
| `BaseDocumentCompressor` | 文档压缩器基类 | 文档压缩基类 |
| `BaseExampleSelector` | 示例选择器基类 | Few-shot 示例选择 |
| `BaseChatLoader` | 聊天加载器基类 | 聊天记录加载 |
| `BaseCrossEncoder` | 交叉编码器基类 | 排序模型基类 |
| `BaseOutputParser` | 输出解析器基类 | 输出解析抽象 |
| `BaseLLMOutputParser` | LLM输出解析器基类 | LLM输出解析抽象 |
| `BaseGenerationOutputParser` | 生成输出解析器基类 | 生成结果解析抽象 |

---

## 二、Agent 相关 / Agent

**重要**：`create_agent` 是工厂函数，不是类。Agent 基于 LangGraph 构建。

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `create_agent` | 创建 Agent（函数） | 创建 Agent 的主入口工厂函数 |
| `AgentState` | Agent 状态 | TypedDict 定义的 Agent 状态结构 |
| `AgentMiddleware` | Agent 中间件 | 扩展 Agent 行为的中间件基类 |
| `AgentAction` | Agent 动作 | Agent 请求执行的动作 |
| `AgentStep` | Agent 步骤 | Agent 执行后的结果步骤 |
| `AgentFinish` | Agent 完成 | Agent 达到停止条件时的返回值 |
| `CompiledStateGraph` | 编译状态图 | Agent 底层基于 LangGraph |

### Agent 中间件 (`agents/middleware/`)

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `TodoListMiddleware` | 待办事项中间件 | 任务规划能力 |
| `HumanInTheLoopMiddleware` | 人工介入中间件 | 人工审批/输入 |
| `ToolRetryMiddleware` | 工具重试中间件 | 工具调用失败重试 |
| `ToolCallLimitMiddleware` | 工具调用限制中间件 | 限制工具调用次数 |
| `ToolSelectionMiddleware` | 工具选择中间件 | LLM 选择调用哪个工具 |
| `ModelRetryMiddleware` | 模型重试中间件 | 模型调用失败重试 |
| `ModelCallLimitMiddleware` | 模型调用限制中间件 | 限制模型调用次数 |
| `PIIMiddleware` | PII 处理中间件 | 个人信息处理 |

### Agent 结构化输出 (`agents/structured_output.py`)

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `ToolStrategy` | 工具策略 | 通过工具返回结构化输出 |
| `ProviderStrategy` | Provider 策略 | 使用模型原生结构化输出 |
| `AutoStrategy` | 自动策略 | 自动选择最佳策略 |

---

## 三、消息系统 / Messages

### 消息类型

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `HumanMessage` | 用户消息 | 用户输入的对话内容 |
| `AIMessage` | AI 消息 | AI 模型的回复 |
| `SystemMessage` | 系统消息 | 系统级指令/配置 |
| `ToolMessage` | 工具消息 | 工具执行结果的消息形式 |
| `FunctionMessage` | 函数消息 | 函数调用结果的消息形式 |
| `ChatMessage` | 聊天消息 | 通用聊天消息 |
| `RemoveMessage` | 删除消息 | 用于删除消息的操作 |

### 消息块 (Message Chunk)

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `AIMessageChunk` | AI 消息块 | 流式输出的 AI 消息片段 |
| `HumanMessageChunk` | 用户消息块 | 用户消息片段 |
| `ToolMessageChunk` | 工具消息块 | 工具消息片段 |
| `FunctionMessageChunk` | 函数消息块 | 函数消息片段 |

### 内容块 (ContentBlock)

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `TextContentBlock` | 文本内容块 | 文本类型的内容 |
| `ImageContentBlock` | 图片内容块 | 图片内容 |
| `AudioContentBlock` | 音频内容块 | 音频内容 |
| `VideoContentBlock` | 视频内容块 | 视频内容 |
| `FileContentBlock` | 文件内容块 | 文件内容 |
| `DataContentBlock` | 数据内容块 | 结构化数据内容 |
| `PlainTextContentBlock` | 纯文本内容块 | 纯文本内容 |
| `ReasoningContentBlock` | 推理内容块 | LLM 的思考过程 |
| `ServerToolCall` | 服务器工具调用 | 服务器端工具调用请求 |
| `ToolCall` | 工具调用 | 工具调用的请求 |
| `InvalidToolCall` | 无效工具调用 | 格式错误的工具调用 |

### 消息工具函数

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `messages_to_dict` | 消息转字典 | 序列化消息 |
| `message_to_dict` | 单条消息转字典 | 单条消息序列化 |
| `messages_from_dict` | 消息从字典构建 | 反序列化消息 |
| `convert_to_messages` | 转换为消息 | 批量转换 |
| `filter_messages` | 过滤消息 | 按条件过滤 |
| `trim_messages` | 裁剪消息 | 减少消息长度 |
| `merge_message_runs` | 合并连续消息 | 合并同类消息 |

---

## 四、Prompt 与模板 / Prompts

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `PromptTemplate` | 提示模板 | 可变量替换的模板 |
| `ChatPromptTemplate` | 聊天提示模板 | 聊天场景的提示模板 |
| `FewShotPromptTemplate` | Few-Shot 提示模板 | 带示例的提示模板 |
| `PromptValue` | 提示词值 | Prompt 的内部表示形式 |
| `BasePromptTemplate` | 提示模板基类 | 模板抽象基类 |

---

## 五、检索与向量 / Retrieval & Vector

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `Retriever` | 检索器 | 根据查询检索文档的组件 |
| `BaseRetriever` | 检索器基类 | 检索器抽象基类 |
| `VectorStoreRetriever` | 向量存储检索器 | 基于向量相似度的检索 |
| `VectorStore` | 向量存储 | 存储向量嵌入的数据库 |
| `BaseStore` | 存储基类 | 键值存储抽象（注意：不是 Vector Store） |
| `InMemoryStore` | 内存存储 | 内存中的键值存储 |
| `InMemoryByteStore` | 内存字节存储 | 内存中的字节存储 |
| `InMemoryCache` | 内存缓存 | 内存中的缓存实现 |
| `InMemoryVectorStore` | 内存向量存储 | 内存中的向量存储 |
| `Embeddings` | 嵌入模型 | 将文本转为向量的模型接口 |
| `BaseCrossEncoder` | 交叉编码器基类 | 排序模型基类 |
| `Document` | 文档 | 被检索的基本单元 |
| `Blob` | 二进制大对象 | 表示二进制数据 |
| `BaseDocumentTransformer` | 文档转换器基类 | 文档处理转换 |

---

## 六、输出解析 / Output Parsing

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `BaseOutputParser` | 输出解析器基类 | 解析 LLM 输出的基类 |
| `StrOutputParser` | 字符串输出解析器 | 解析为字符串 |
| `JsonOutputParser` | JSON 输出解析器 | 解析为 JSON |
| `SimpleJsonOutputParser` | 简单 JSON 输出解析器 | 无 schema 的 JSON |
| `PydanticOutputParser` | Pydantic 输出解析器 | 解析为 Pydantic 模型 |
| `PydanticToolsParser` | Pydantic 工具解析器 | 解析工具参数为 Pydantic |
| `XMLOutputParser` | XML 输出解析器 | 解析为 XML |
| `ListOutputParser` | 列表输出解析器 | 基类 |
| `CommaSeparatedListOutputParser` | 逗号分隔列表解析器 | 解析逗号分隔的列表 |
| `MarkdownListOutputParser` | Markdown 列表解析器 | 解析 Markdown 列表 |
| `NumberedListOutputParser` | 编号列表解析器 | 解析编号列表 |

---

## 七、回调与监控 / Callbacks & Tracing

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `Callback` | 回调 | 事件通知机制 |
| `CallbackManager` | 回调管理器 | 管理回调处理器的管理器 |
| `BaseCallbackHandler` | 回调处理器基类 | 事件处理基类 |
| `CallbackManagerForLLMRun` | LLM 运行回调管理器 | LLM 调用的回调 |
| `CallbackManagerForChainRun` | 链运行回调管理器 | 链调用的回调 |
| `CallbackManagerForToolRun` | 工具运行回调管理器 | 工具调用的回调 |
| `CallbackManagerForRetrieverRun` | 检索器运行回调管理器 | 检索器调用的回调 |
| `UsageMetadata` | 使用量元数据 | API 调用用量统计 |
| `InputTokenDetails` | 输入 Token 详情 | 输入的 token 详情 |
| `OutputTokenDetails` | 输出 Token 详情 | 输出的 token 详情 |

---

## 八、链式调用 / Chaining (Runnable)

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `RunnableSequence` | 顺序链 | 多个 Runnable 顺序执行 |
| `RunnableParallel` | 并行链 | 多个 Runnable 并行执行（也称 `RunnableMap`） |
| `RunnableBranch` | 分支链 | 条件分支选择 |
| `RunnableWithFallbacks` | 降级链 | 主链失败时使用备选 |
| `RunnableWithMessageHistory` | 消息历史链 | 自动管理对话历史 |
| `RunnablePassthrough` | 直通链 | 不做任何处理直接传递输入 |
| `RunnableAssign` | 赋值链 | 为配置赋值 |
| `RunnablePick` | 选择链 | 从输入中选择特定字段 |
| `RunnableRetry` | 重试链 | 失败时自动重试 |
| `RunnableConfig` | 运行配置 | TypedDict 定义的配置 |
| `chain()` | 链函数 | 将多个 Runnable 组合的装饰器/函数 |

---

## 九、工具系统 / Tool System

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `BaseTool` | 工具基类 | 工具的抽象基类 |
| `BaseToolkit` | 工具包基类 | 工具集合的基类 |
| `Tool` | 简单工具 | 使用 `@tool` 装饰器创建的简单工具 |
| `StructuredTool` | 结构化工具 | 带参数结构的工具 |
| `ToolException` | 工具异常 | 工具执行时的异常 |
| `InjectedToolArg` | 注入的工具参数 | 运行时注入的工具参数 |
| `InjectedToolCallId` | 注入的工具调用 ID | 工具调用标识 |
| `@tool` | 工具装饰器 | 将函数转为工具的装饰器 |
| `create_schema_from_function` | 从函数创建 schema | 从函数签名生成工具参数 schema |
| `convert_runnable_to_tool` | Runnable 转工具 | 将 Runnable 转换为工具 |
| `create_retriever_tool` | 创建检索器工具 | 从 Retriever 创建工具 |

---

## 十、文档处理 / Document Processing

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `Document` | 文档 | 被检索的基本单元 |
| `Blob` | 二进制大对象 | 表示二进制数据（图片、文件等） |
| `BaseMedia` | 媒体基类 | 提供 id 和 metadata 的基类 |
| `BaseDocumentTransformer` | 文档转换器基类 | 文档转换处理基类 |
| `BaseLoader` | 加载器基类 | 从各种来源加载文档 |
| `BaseBlobParser` | Blob 解析器基类 | 解析 Blob 为文档 |
| `TextContentBlock` | 文本内容块 | 文本类型的内容块 |
| `ImageContentBlock` | 图片内容块 | 图片内容 |
| `FileContentBlock` | 文件内容块 | 文件内容 |

---

## 十一、序列化 / Serialization

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `Serializable` | 可序列化 | 支持序列化的标记接口 |
| `RunnableSerializable` | 可序列化运行单元 | Runnable 的序列化实现 |
| `lc_serializable` | LangChain 可序列化 | 类级别的序列化标记 |
| `is_lc_serializable` | 是否可序列化 | 检查类是否可序列化 |

---

## 十二、LLM 与模型 / Models

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `BaseChatModel` | 聊天模型基类 | 聊天模型抽象基类 |
| `BaseLLM` | 大语言模型基类 | LLM 抽象基类 |
| `init_chat_model` | 初始化聊天模型 | 工厂函数，创建聊天模型实例 |
| `AIMessage` | AI 消息 | 模型生成的回复 |
| `AIMessageChunk` | AI 消息块 | 流式响应片段 |
| `ChatMessage` | 聊天消息 | 通用聊天消息 |
| `UsageMetadata` | 使用量元数据 | Token 使用量统计 |

---

## 十三、错误处理 / Error Handling

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `LangChainException` | LangChain 异常 | 框架基础异常类 |
| `OutputParserException` | 输出解析异常 | 解析 LLM 输出失败 |
| `TracerException` | 追踪器异常 | 追踪相关异常 |
| `ContextOverflowError` | 上下文溢出错误 | 输入超出模型上下文限制 |
| `ToolException` | 工具异常 | 工具执行失败 |
| `SchemaAnnotationError` | Schema 注解错误 | 工具参数 schema 定义错误 |
| `StructuredOutputError` | 结构化输出错误 | 结构化输出相关错误 |
| `MultipleStructuredOutputsError` | 多结构化输出错误 | 多个冲突的结构化输出配置 |

---

## 十四、存储与记忆 / Storage & Memory

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `BaseStore` | 存储基类 | 键值存储抽象接口 |
| `InMemoryStore` | 内存存储 | 基于内存的键值存储 |
| `InMemoryByteStore` | 内存字节存储 | 基于内存的字节存储 |
| `BaseChatMessageHistory` | 聊天历史基类 | 对话历史存储抽象 |
| `InMemoryChatMessageHistory` | 内存聊天历史 | 基于内存的聊天历史 |
| `InMemoryCache` | 内存缓存 | 基于内存的缓存 |

---

## 十五、配置与可配置性 / Configuration

| English | 中文翻译 | 说明 |
|---------|----------|------|
| `RunnableConfig` | 运行配置 | TypedDict，运行时配置 |
| `ConfigurableField` | 可配置字段 | 标识可动态配置的字段 |
| `ConfigurableFieldSingleOption` | 单选项可配置字段 | 从多个选项中选择一个 |
| `ConfigurableFieldMultiOption` | 多选项可配置字段 | 从多个选项中选择多个 |
| `ConfigurableFieldSpec` | 可配置字段规范 | 字段规范的描述 |
| `Configurable` | 可配置的 | 标识类支持动态配置 |

---

## 十六、常用动词/短语

| English | 中文翻译 | 语境 |
|---------|----------|------|
| invoke | 调用 | 调用 Runnable |
| ainvoke | 异步调用 | 异步调用 Runnable |
| batch | 批量调用 | 批量执行多个输入 |
| abatch | 异步批量调用 | 异步批量执行 |
| stream | 流式调用 | 逐步返回输出 |
| astream | 异步流式调用 | 异步逐步返回 |
| pipe / `\|` | 管道连接 | LCEL 链式调用 |
| bind | 绑定 | 绑定配置或参数 |
| compose | 组合 | 组合多个组件 |
| retrieve | 检索 | 从检索器获取文档 |
| parse | 解析 | 解析 LLM 输出 |
| render | 渲染 | 渲染 Prompt 模板 |
| yield | 生成 | 生成器 yields 输出 |
| merge | 合并 | 合并多个配置/消息 |
| split | 分割 | 分割文本或任务 |
| filter | 过滤 | 过滤不相关的结果 |
| transform | 转换 | 转换数据格式 |
| aggregate | 聚合 | 聚合多个结果 |
| ensure_config | 确保配置 | 获取并验证配置 |

---

## 十七、不存在的类名（请勿使用）

以下是在 LangChain 代码中**不存在的类名**，如果 AI 翻译时遇到这些名称，请修正为正确的类名：

| 错误名称 | 正确类名 | 说明 |
|----------|----------|------|
| `AgentExecutor` | `create_agent` 函数 | Agent 通过工厂函数创建 |
| `Plan` | `PlanningState` / `TodoListMiddleware` | 规划相关但无独立 Plan 类 |
| `MultiModalContent` | `ImageContentBlock` 等 | 使用具体的内容块类型 |
| `TextContent` | `TextContentBlock` | 少了一个 Block |
| `ChatCompletion` | `AIMessage` | 使用 AIMessage |
| `MessageGeneration` | `AIMessage` | 使用 AIMessage |
| `CompletionModel` | `BaseChatModel` | 使用 BaseChatModel |
| `ToolResult` | `ToolMessage` | 工具结果用 ToolMessage 表示 |
| `ToolName` | 无直接对应 | 工具名称就是字符串 |
| `ToolSchema` | `ArgsSchema` 或函数签名 | 使用 ArgsSchema |
| `ToolDefinition` | `BaseTool` 或 dict | 工具定义 |
| `ConfigField` | `ConfigurableField` | 少了一个字母 C |
| `Passthrough` | `RunnablePassthrough` | 完整的类名 |
| `FallbackChain` | `RunnableWithFallbacks` | 完整的类名 |
| `RouterChain` | `RouterRunnable` / `RunnableBranch` | 使用具体的类 |
| `SequentialChain` | `RunnableSequence` | 完整的类名 |

---

## 贡献指南

如果您发现某些翻译不准确或有更好的建议，欢迎提出。
